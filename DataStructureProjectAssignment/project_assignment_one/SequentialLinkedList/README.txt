This will hold the sequential implementations of our Linked List data structure.

To compile the C++ implementation from your terminal or command line, run the
run.sh bash file by calling

bash run.sh
